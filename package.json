{
  "name": "todolist",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/plugins/*"
  ],
  "scripts": {
    "start": "npm start -w @todolist/app",
    "build": "npm run build -w @todolist/app",
    "test": "npm test -ws -- --watchAll=false"
  },
  "devDependencies": {
    "jest": "^27.5.1",
    "typescript": "^4.6.3"
  },
  "dependencies": {
    "@types/react-helmet": "^6.1.5",
    "react-helmet": "^6.1.0"
  }
}
